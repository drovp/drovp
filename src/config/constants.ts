import manifest from 'manifest';

export const PLUGIN_KEYWORD = `${manifest.name}plugin`;
export const DEPENDENCY_KEYWORD = `${manifest.name}dependency`;
export const UPDATE_LOG_FILE = 'update.log';
export const UPDATE_ERROR_LOG_FILE = 'update-error.log';
